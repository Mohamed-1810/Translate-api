const googlettsapi = require("./index")
const path = require("path")

console.time("start")

googlettsapi.get(undefined, {
    text: "Darius is a gay female 69 of age",
    output: path.join(__dirname, "temp.aac"),
})
.then(() => {
    console.timeEnd("start")
})
.catch((err) => {
    console.log(err)
})